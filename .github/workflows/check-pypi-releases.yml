name: Check PyPI Releases

on:
  workflow_call:
    outputs:
      l402_python:
        description: "True if L402sdk Python bindings version was bumped"
        value: ${{ jobs.check.outputs.l402_python }}
      l402_langchain:
        description: "True if l402-langchain version was bumped"
        value: ${{ jobs.check.outputs.l402_langchain }}

jobs:
  check:
    name: Detect version bumps
    runs-on: ubuntu-latest
    outputs:
      l402_python: ${{ steps.check-versions.outputs.l402_python }}
      l402_langchain: ${{ steps.check-versions.outputs.l402_langchain }}
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2

      - name: Compare versions
        id: check-versions
        run: |
          prev_commit="${{ github.event.before }}"
          echo "Previous commit: $prev_commit"

          get_toml_version() {
            grep -E '^version\s*=' "$1" | head -1 | sed -E 's/version\s*=\s*"([^"]+)"/\1/'
          }

          # --- L402sdk (Python bindings) ---
          py_toml="crates/l402-python/pyproject.toml"
          current_py=$(get_toml_version "$py_toml")
          previous_py=$(git show "$prev_commit:$py_toml" | grep -E '^version\s*=' | head -1 | sed -E 's/version\s*=\s*"([^"]+)"/\1/')
          echo "L402sdk (python): $previous_py → $current_py"

          if [ "$current_py" != "$previous_py" ]; then
            echo "L402sdk python version bump detected."
            echo "l402_python=true" >> "$GITHUB_OUTPUT"
          else
            echo "L402sdk python: no version bump."
            echo "l402_python=false" >> "$GITHUB_OUTPUT"
          fi

          # --- l402-langchain ---
          lc_toml="packages/l402-langchain/pyproject.toml"
          current_lc=$(get_toml_version "$lc_toml")
          previous_lc=$(git show "$prev_commit:$lc_toml" | grep -E '^version\s*=' | head -1 | sed -E 's/version\s*=\s*"([^"]+)"/\1/')
          echo "l402-langchain: $previous_lc → $current_lc"

          if [ "$current_lc" != "$previous_lc" ]; then
            echo "l402-langchain version bump detected."
            echo "l402_langchain=true" >> "$GITHUB_OUTPUT"
          else
            echo "l402-langchain: no version bump."
            echo "l402_langchain=false" >> "$GITHUB_OUTPUT"
          fi
